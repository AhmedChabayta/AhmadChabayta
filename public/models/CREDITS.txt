Active model (rendered by src/components/site/tree3d.tsx):
  public/models/source/olive-tree/olive-tree.obj  (+ .mtl, textures)

User-supplied olive tree (Blender export). Textures are wired in
code because the .mtl references dead absolute Windows paths:
  - olive-leaves    -> olive-branch-cards{.jpg,-alpha.jpg,-normal.jpg}
  - olive-tree-bark -> tree-specular.png (base), tree-normal.png

Add the model's real source/author/license here for attribution.

To swap the tree: replace olive-tree.obj (+ its textures) in that
folder, or point OBJ_URL/BASE in tree3d.tsx at a new path. The
renderer auto-stands, centers, scales, lights, shadows and winds it.

NOTE: tree-normal.png / tree-specular.png are ~10 MB each (PNG).
For faster loads these should be downscaled to ~2K. Ask me to do it.
